#!/usr/bin/env bash
# Installs nginx web-01 server

sudo apt-get  update
sudo apt-get  install nginx
sudo ufw allow 'Nginx HTTP'

echo "Hello World!" | sudo tee /var/www/html/index.nginx-debian.html
sudo nginx -t
sudo service nginx start
